#! /bin/bash

database='appvno'
refLink='https://appvno-ws.multidemos.com/api/notification/send/pushNotification'
authorization='Authorization: Basic YXBwdm5vdXNlcjphcHB2bm9wYXNz'
# contentType=$"Content-Type: application/json"
expireDate=$(date -d "+3 days")
# updatedaw=$(psql -d $database -c "UPDATE notif_counter SET status = 'Sent request' WHERE mobile=$list")
test=$( curl --location --request POST $refLink \
--header 'Authorization: Basic YXBwdm5vdXNlcjphcHB2bm9wYXNz' \
--header 'Content-Type: application/json' \
--data-raw '{
"title":"27th day: Subscription is about to expire",
"body":"Your Philippines Mobile Number plan is about to expire on $expireDate.Please renew now to continue receiving calls and text message form Philippines.",
"min" : [
{"mobileNumber" : "$list"}
],
"type" : "Notification",
"action_type" : "NotificationActivity"
}')

for list in $(cat list.txt)
do
        echo "$test"
        psql -d $database -c "UPDATE notif_counter SET status='sent' WHERE mobile='$list'"
done
