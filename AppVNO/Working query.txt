SELECT * FROM svn WHERE DATE(date_subscribed) = NOW()::DATE;

SELECT expiry_date::DATE - date_subscribed::DATE as daysin FROM svn WHERE expiry_date::DATE - date_subscribed::DATE = 3;

SELECT id FROM svn WHERE expiry_date::DATE - date_subscribed::DATE = 3;

UPDATE svn SET date_subscribed = NOW() WHERE status='ACTIVE';

UPDATE svn SET expiry_date = NOW() + INTERVAL '1 MONTH' WHERE status='ACTIVE';

SELECT id, expiry_date::DATE - current_date as daysin FROM svn WHERE status='ACTIVE';

SELECT id, current_date - expiry_date::DATE as daysin FROM svn WHERE status='ACTIVE';

SELECT id FROM svn WHERE expiry_date::DATE - current_date = 31;